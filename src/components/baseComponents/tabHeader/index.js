import TabHeader from './tabHeader';
// import  {connect} from 'react-redux';
// import { bindActionCreators } from 'redux';

// const mapStateToProps = (state) => {
//     return {
//         activeAssignmentCount:state.dashboardReducer.count.activeAssignmentCount,
//         inactiveAssignmentCount:state.dashboardReducer.count.inactiveAssignmentCount,
//         visitStatusCount:state.dashboardReducer.count.visitStatusCount,
//         timesheetCount:state.dashboardReducer.count.timesheetCount,
//         contractNearExpiryCount:state.dashboardReducer.count.contractNearExpiryCount,
//         documentAprovalCount:state.dashboardReducer.count.documentAprovalCount
//     }
// };

// export default connect(mapStateToProps)(TabHeader);
export default TabHeader;